import Featured from "../components/Featured";
import Hero from "../components/Hero";

const Home = () => {
  return (
    <section className="relative">
      <Hero />
      <Featured />
    </section>
  );
};

export default Home;
